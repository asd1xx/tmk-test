<!DOCTYPE html>

<title>Просмотр статьи</title>

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div>
    <a href="{{ path('app_article_index') }}">Статьи</a>
</div>

<div>
    <h1>Информация о статье</h1>
    <table>
        <thead>
            <tr>
                <th>ID</th>
                <th>Заголовок</th>
                <th>Уникальный код</th>
                <th>Активность</th>
                <th>Просмотры</th>
                <th>Описание</th>
                <th>Дата создания</th>
            <tr>
        </thead>
        <tbody>
                <tr>
                    <td>{{ article.id }}</td>
                    <td>{{ article.title }}</td>
                    <td>{{ article.uid }}</td>
                    <td>{{ article.activity }}</td>
                    <td>
                        {% if article.views <= 999 %}
                            {{ article.views }}
                        {% elseif article.views > 999 %}
                            {{ '1K' }}
                        {% elseif article.views > 999999 %}
                            {{ '1M' }}
                        {% elseif article.views > 999999999 %}
                            {{ '1B' }}
                        {% endif %}
                    </td>
                    <td>{{ article.description }}</td>
                    <td>{{ article.createdAt ? article.createdAt|date('Y-m-d H:i:s') : '' }}</td>
                </tr>
        </tbody>
    </table>
</div>
{% endblock %}